# Chaine de connexion Neon, pour le deploiement.
# Ce fichier-ci est un modele ; le vrai `.env.prod` est ignore par git.
#
# Prends la connection string "direct" (non poolee) dans le tableau de bord
# Neon : on gere deja notre propre pool cote application.
DATABASE_URL=postgresql://user:motdepasse@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
